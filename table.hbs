<section class="informative">
  <h1>{{table.title}}</h1>
  <p>
    The results are shown below:
  </p>
      {{#each tests}}
        <section>
          <h2>{{@key}}</h2>
          <table class="simple">
            <style>
              td.subtest {
                padding-left: 2rem;
              }
              td.optional {
                background-color: #b9b7b7;
              }
            </style>
            <thead>
              <th width="80%">Test</th>
                {{#each @root.columnNames}}
                  <th>{{this}}</th>
                {{/each}} 
            </thead>
            <tbody>
        {{#each this}}
          <tr>
            <!--This is the name of the test-->
            <td class="subtest">{{@key}}</td>
            {{#each this}}
              <!--These contain if the test passed, failed, or was skipped-->
              <td class="{{state}} {{getOptional optional}}">{{getStatusMark state}}</td>
            {{/each}}
          </tr>
        {{/each}}
          </tbody>
          </table>
        </section>
      {{/each}}
</section>
