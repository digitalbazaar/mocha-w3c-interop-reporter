const pending = '🛑';
const passing = '✓';
const failed = '❌';
const accessDenied = '⊘';
const timeout = '⏱';
const notImplemented = '—';

module.exports = {
  pending,
  passing,
  failed,
  accessDenied,
  timeout,
  notImplemented
};
